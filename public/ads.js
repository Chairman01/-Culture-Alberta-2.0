/* Decoy. This file exists only so the ad-block monitor can test whether a
   request to an ad-shaped URL succeeds — filter lists block "/ads.js" by name.
   It is never executed for any purpose and intentionally does nothing. */
window.__adProbeLoaded = true
